{% extends 'lazy.html.twig' %}

{% block title %}ReclamationCategory index{% endblock %}

{% block body %}
    <h1>ReclamationCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reclamation_category in reclamation_categories %}
            <tr>
                <td>{{ reclamation_category.id }}</td>
                <td>{{ reclamation_category.nom }}</td>
                <td>
                    <a href="{{ path('app_reclamation_category_show', {'id': reclamation_category.id}) }}">show</a>
                    <a href="{{ path('app_reclamation_category_edit', {'id': reclamation_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_category_new') }}">Create new</a>
{% endblock %}
