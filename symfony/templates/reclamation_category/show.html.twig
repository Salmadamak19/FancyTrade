{% extends 'lazy.html.twig' %}

{% block title %}ReclamationCategory{% endblock %}

{% block body %}
    <h1>ReclamationCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation_category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reclamation_category.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_category_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_category_edit', {'id': reclamation_category.id}) }}">edit</a>

    {{ include('reclamation_category/_delete_form.html.twig') }}
{% endblock %}
