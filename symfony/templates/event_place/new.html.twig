{% extends 'lazy.html.twig' %}

{% block title %}New EventPlace{% endblock %}

{% block body %}
            <div class="container-xxl flex-grow-1 container-p-y">
              <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Home/</span> Add Event Place</h4>

              <!-- Basic Layout & Basic with Icons -->
              <div class="row">
                <!-- Basic Layout -->
                <div class="col-xxl">
                  <div class="card mb-4">
                    <div class="card-header d-flex align-items-center justify-content-between">
                      <h5 class="mb-0">Event</h5>
                    </div>
                    <div class="card-body">
    {{ include('event_place/_form.html.twig') }}

    <a href="{{ path('app_event_place_index') }}">back to list</a>
                        </div>
                  </div>
                </div>
              </div>
            </div>
    
{% endblock %}
